# Runtime dependencies (kept in sync with pyproject.toml; `pip install -e .` is equivalent).
flask>=3.0,<4
flask-cors>=4.0
werkzeug>=3.0,<4
numpy>=1.24
scipy>=1.10
soundfile>=0.12
soxr>=0.3
waitress>=2.1
