## Summary

<!-- What does this change and why? Link the issue if there is one. -->

## Checklist

- [ ] `ruff check` and `ruff format --check` pass
- [ ] `pytest` passes (add tests for new behaviour)
- [ ] Docs updated (README / docs/*.md / CHANGELOG.md) where behaviour changed
- [ ] Fingerprint-affecting change? `FINGERPRINT_ALGORITHM_VERSION` bumped and the CHANGELOG says re-indexing is required
